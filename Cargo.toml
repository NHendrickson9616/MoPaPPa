[package]
name = "mopappa"
version = "0.1.0"
edition = "2024"

[dependencies]
tiktoken-rs = "0.12.0"

[dev-dependencies]
syn = { version = "3", default-features = false, features = ["full", "parsing"] }
